[project]
name = "api-demo-deployment"
version = "1.0"
description = "for deployment purposes"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.138.0",
    "pydantic[email]>=2.13.4",
    "uvicorn>=0.49.0",
]
